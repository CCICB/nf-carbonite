config {
    testsDir "tests"
    workDir ".nf-test"
    configFile "nextflow.config"
    profile "test"

    // Symlink the bundled test data into the test working directory so the
    // relative FASTQ-directory path inside assets/test_data/samplesheet.csv resolves
    stage {
        symlink "assets"
    }
}
